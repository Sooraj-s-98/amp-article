"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _apply = _interopRequireDefault(require("./apply"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const createSelector = (...args) => {
  const selectors = args.slice(0, args.length - 1);
  const selector = args[args.length - 1];

  const out = cb => {
    const next = async (...results) => {
      const result = await selector(...results);
      return await cb(result);
    };

    const result = (0, _apply.default)(...selectors, next);
    return result;
  };

  out._selectors = selectors;
  out._selector = selector;
  return out;
};

var _default = createSelector;
exports.default = _default;
//# sourceMappingURL=createSelector.js.map