"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _parseurl = _interopRequireDefault(require("parseurl"));

var _qs = require("qs");

var _util = require("./util");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const isMatch = (pred, obj) => {
  if (Array.isArray(obj)) {
    return obj.some(x => isMatch(pred, x));
  } else if (Array.isArray(pred)) {
    return pred.some(x => isMatch(x, obj));
  } else if (typeof pred === 'object') {
    // TODO: FIXME: Flow requires this assignment.
    const index = pred;
    return Object.keys(pred).every(key => {
      return isMatch(index[key], obj[key]);
    });
  }

  return pred === obj;
};

var _default = query => (0, _util.create)(req => isMatch(query, (0, _qs.parse)((0, _parseurl.default)(req).search)));

exports.default = _default;
//# sourceMappingURL=query.js.map